{
  "name": "care-gp-public",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Clean-room, non-clinical TypeScript demo of synthetic primary healthcare operations admin workflows.",
  "bin": {
    "care-gp-public": "dist/cli.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "start": "node dist/cli.js demo",
    "validate": "npm run build && node dist/cli.js validate",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
